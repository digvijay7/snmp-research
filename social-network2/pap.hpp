#include"log.hpp"
#include"graph.hpp"
#include<vector>

// Static Function definiton to get log data and populate a graph using it

namespace sn{
  static bool process_n_populate(std::vector<log> &data, graph & g);
}

